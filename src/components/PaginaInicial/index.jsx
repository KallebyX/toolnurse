import React from 'react';
import Menu from '../Menu';
import Formulario from '../RecemNascido';

const PaginaInicial = () => {
    return (
        <div>
            <Menu />
            <Formulario />
        </div>
    );
};

export default PaginaInicial;
